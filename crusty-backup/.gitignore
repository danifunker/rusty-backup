build/
*.exe
deps/zlib/
deps/lz4/
deps/dl/
net/watt32/
net/dl/
net/mtcp/
# net/drivers/*.com are now VENDORED (the Crynwr GPL collection, fetched by
# net/fetch-drivers.sh and committed) so the CI-built media ships with drivers.
